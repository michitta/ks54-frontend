import { FC } from "react";

export const SelectedAction: FC<String> = (data) => {
    return (
        data === "groups" ?
            <div>

            </div> :
            <div>

            </div>
    )
}